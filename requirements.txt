# --- 基礎數據處理 ---
pandas
numpy
matplotlib

# --- 數據獲取（核心）---
yfinance
requests
lxml          # pandas.read_html 解 TWSE JSP 用
tqdm

# --- 通知（暫關但 import 還在）---
resend

# --- 台股 fallback 用 ---
akshare

# --- Streamlit Cloud webapp（路徑 5）---
# 合併進主 requirements.txt 的原因：Streamlit Cloud 新版 UI 移除了
# "Python dependencies file" 欄位，強制只看 requirements.txt。
# GH Actions main pipeline 會多裝這幾個（~20 秒），不影響功能。
streamlit>=1.32
plotly>=5.18

# --- Phase 3：Gemini AI 智能摘要 ---
# Free tier，需在 GH Secrets 設 GEMINI_API_KEY
# 沒設時 ai_summary 模組會自動跳過
google-generativeai>=0.8
